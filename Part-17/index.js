/*
    const temp = -100;

    if (temp > 0 && temp <= 320) {
        console.log("The weather is Good")
    }
    else {
        console.log("The weather is Bad");
    }
*/

/*
    const temp = -250;

    if (temp <= 0 || temp > 30) {
        console.log("The weather is Bad")
    }
    else {
        console.log("The weather is Good");
    }
*/

    const isSunny = true;

    if (!isSunny) {
        console.log("It is Clody")
    }
    else {
        console.log("It is Sunny");
    }
